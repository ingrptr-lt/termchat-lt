# 1. The Core Framework
streamlit

# 2. AI Libraries
# OpenAI SDK is here if you want to use OpenAI, 
# but we are using 'requests' for Groq in app.py to avoid version conflicts.
openai>=1.0.0

# 3. Utilities
python-dotenv

# 4. MQTT (REQUIRED for Streamlit Backend to talk to Frontend)
paho-mqtt>=2.0.0
